@model WebApplication2.view_model.ordervm

@{
    ViewData["Title"] = "create";
    SelectList plist = new SelectList(ViewBag.plist, "id", "name");    
}

<h1>create</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="save" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="productid" class="control-label"></label>
                <select asp-for="productid" class="form-control" asp-items="plist">
                    <option>--select--</option>
                </select>
                <span asp-validation-for="productid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="qantity" class="control-label"></label>
                <input asp-for="qantity" class="form-control" />
                <span asp-validation-for="qantity" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<br />
<div>
    <a asp-action="Index" class= "btn btn-success">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
